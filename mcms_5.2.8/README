后台 http://127.0.0.1:8080/ms/ 账号密码：msopen msopen
docker-compose up -d 
docker exec  -it docker_images-web-1 bash
source /etc/profile
cd mcms-5.2.8-jar
./start.sh  #开启时间可能有点久。如果开启不了，执行./stop.sh后执行./start.sh

